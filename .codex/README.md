# Espelho Codex

Esta pasta representa a estrutura-alvo da adaptação do `Faz Acontecer` para `Codex`.

Ela existe para:
- espelhar o modelo de `skills` e `config.toml` do GSD no Codex
- organizar a adaptação PT-BR
- preservar a estrutura esperada por esse runtime

## Estrutura esperada

- `skills/`
- `agents/`
- `config.toml`
- `get-shit-done/`
